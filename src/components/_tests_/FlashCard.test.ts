/*import { shallowMount } from '@vue/test-utils';
import FlashCard from '../FlashCard.vue';*/

describe('FlashCard.vue', () => {
    test('sanity test', () => {
        return
    });
    
    /*it('renders when mounted', () => {
        // Need to mock the Vuex  store to render it

        const wrapper = shallowMount(FlashCard);
        expect(wrapper.text()).toMatch(`Montrer la traduction`);
    });*/

    /*it('shows a button to display the translation', function() {
        //do something
        expect(true === true);
    });*/
})